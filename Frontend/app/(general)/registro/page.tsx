import RegisterForm from "./components/RegisterForm";

// pagina de registro de donde se muestra solo la forma creeria yo
export default function RegistroPage() {
  return (
    <main>
      <RegisterForm />
    </main>
  );
}